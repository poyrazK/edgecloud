//! Project scaffolding helpers — `edge init` and friends.
//!
//! The `wit` submodule materializes the canonical WIT tree into
//! a freshly scaffolded project so the project builds offline
//! outside the monorepo (issue #576).

pub mod wit;
